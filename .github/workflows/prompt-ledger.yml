name: Prompt Ledger Check

on:
  pull_request:

jobs:
  prompt-ledger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check prompt ledger
        run: ./scripts/check-prompts.sh
